## Vectors


#1 - Added basic vectors operations

Started with a function to display the changes with printVector.
Made basic addition and scalar functions that create a new vector with the new properties.
Combined addition and scalars to build linear combinations.
Added dot product, vector length calculator, and unit vector functions to finish out the basic vector operations.

-Questions/Concerns:
Don't know if I should change the vector's elements rather than make a new vector.

Currently using built-in vectors. Should I make a separate vector class instead?

-Ideas:
Make a matrix class.
Build a transpose function.
Maybe tackle matrix multiplication?
